# Rollback plan

## Baseline

The functional baseline is commit `609270f533666e7636d11f7d16590be925ec836f`. The implementation branch is `1.4`; the unrelated modified `.github/ISSUE_TEMPLATE/bug-report.yml`, untracked `.codegraph/`, and untracked `docs/PackForge_Missing_Implementation_Plan.md` must remain outside functional commits.

## Rules

- Use the chronological map in `stable-checkpoints.md`.
- Revert verified functional units with normal `git revert <sha>`.
- Do not reset, discard, amend, squash, rebase away, or force-push verified checkpoints.
- Keep legacy build tasks available until Stonecutter current-matrix parity is proven.
- Keep per-target capability exclusions and per-feature defaults as rollback controls.
- If an optional path fails, return to vanilla/original control flow without disabling unrelated capabilities.

## Migration rollback points

1. Before registry schema migration: baseline HEAD above.
2. Before Stonecutter switch: last verified parallel-build checkpoint.
3. Before each capability migration: immediately preceding checkpoint.
4. Before each range consolidation/default promotion: preceding exact-release proof checkpoint.
5. Final rollback: revert the latest named checkpoint(s) in reverse dependency order.

## Latest checkpoints

- `6ac88581dbd4b1fd938a84396bb17e96a964d4c3` (parent `86dfcd77a5b571e49f796e49039984a206d1eacd`): Phase K resolved-resource hash binding. Revert later documentation dependants first, then `git revert 6ac8858`. Controlled Fabric/Forge/NeoForge smokes now require positive deterministic resolved-resource hashes, base Fabric controller rows stage immutable deterministic fixtures, and the exact matrix stores/resume-validates the uppercase hash token with helper-bound harness identity. Helper, matrix resume, profile transport, focused Gradle, aggregate contract, parse, and independent Luna review passed. No Minecraft, final-JAR runtime, 62-cell matrix, or release proof belongs to this checkpoint; Phase K remains `PARTIAL`.
- `c696253832a005ffbbccf838e52a639ba1658954` (parent `90ed4eababa35aecb151d2b5eeca33705c7abfeb`): Phase G replacement-stage configuration safety test. Revert later documentation dependants first, then `git revert c696253` to remove only this test. Offline Fabric 1.21.1 `PackForgeConfigPreservationTest` reached BUILD SUCCESSFUL in 54 seconds across ten executed tasks; independent 5.6-sol-wm review passed. No production source, clean build, live UI route, renderer-family client run, shader/atlas profile, matrix, or release proof belongs to this checkpoint; Phase G remains `IMPLEMENTED_UNVERIFIED`.
- `90ed4eababa35aecb151d2b5eeca33705c7abfeb` (parent `1d03c6dcd4bd010bde9ccd9d2d326e8f95ef7a97`): whole-plan report reconciliation across `compatibility-matrix.md`, `implementation-report.md`, `quick-pack-compatibility.md`, `rollback.md`, `stable-checkpoints.md`, and `work-log.md`. Revert `c696253` and later documentation dependants first, then `git revert 90ed4ea`; revert `1d03c6d` afterward only if also removing the canonical audit. Independent documentation review passed after clarifying the `71f29b` historical baseline. No build, package, runtime, profile, benchmark, or publication belongs to this checkpoint.
- `1d03c6dcd4bd010bde9ccd9d2d326e8f95ef7a97` (parent `42cfd18342abad235a95650bdb74869fb23801f5`): canonical whole-plan A-L audit. Revert later documentation dependants first, then `git revert 1d03c6d` to restore the previous Phase E-focused audit. It uses the assignment's five statuses and five-column evidence schema, records D and J complete, B/C/G/H implemented-unverified, A/E/F/I/K/L partial, and binds the current artifact failure (20 expected, 17 present, five matching, 15 missing, 12 stale, manifest absent). Focused audit checks and independent review passed; no clean build or runtime proof belongs to this checkpoint.
- `0138d6edbad4cebd45773050d81e7897dbbfb7b3` (parent `9ab73924b87b025553a871968c62d2b1a613e07a`): extend the Phase E canonical preprocessed SimpleReload Fabric seam to 1.21.5. Revert newest-first: revert later dependants, then `git revert 0138d6e`; then `b1ee885`, `731d1c2`, `f74e0a7`, `aba98b3`, `c5b61e2`, `7efd43f`, `4f82154`, and `b54a40f` if removing the preceding Fabric native work. Direct contract passed across 53 cells with four native SimpleReload cells. Focused offline 1.21.4/1.21.5/1.21.6 compile/sourceJar passed in 51 seconds across 18 executed tasks; 1.21.5 and 1.21.6 generated/source-JAR entries matched `B2F6321B...` with 4,648-byte classes, while 1.21.4 retained its inactive generated source and 4,641-byte physical class. Metrics remained 217 files / 16,575 LOC, zero duplicate groups, and ten blocks / maximum 40 lines; validator target-key literals increased from 102 to 103. Independent review passed with no required fixes. No runtime, all-53 build/package parity, full matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `b1ee885` (parent `ebb7c41`): Phase E canonical preprocessed SimpleReload Fabric seam. Revert newest-first: revert later dependants, then `git revert b1ee885`; then `731d1c2`, `f74e0a7`, `aba98b3`, `c5b61e2`, `7efd43f`, `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates SimpleReload for exact Fabric `mc1_21_6`, `mc1_21_7`, and `mc1_21_8` through adapter plus source policy; adjacent/all other eras remain physical. Static self-test passed 106 mutations; metrics passed in 30 seconds with 217 files / 16,575 LOC, 110 bridge files / 5,555 LOC (33.51%), zero duplicates, ten blocks / maximum 40 lines, and 18 platform target-key references. Focused 1.21.5/1.21.6/1.21.8/1.21.9 compile/sourceJar reached BUILD SUCCESSFUL in 46 seconds across 24 tasks (20 executed, four up-to-date); active `C30B2016...` matched, inactive physical `BB0AA935...` / `E94AAB4F...` remained, and classes existed. Independent final review passed. No runtime, all-53 parity, full matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `731d1c2` (parent `9caa86e`): Phase E canonical preprocessed LoadingOverlayToast Fabric seam. Revert newest-first: revert later dependants, then `git revert 731d1c2`; then `f74e0a7`, `aba98b3`, `c5b61e2`, `7efd43f`, `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates canonical LoadingOverlayToast preprocessing for exact active Fabric common 1.20.5 through 1.21.5 (eight targets, guard 18) and modern 1.21.6 through 1.21.11 (six targets, guard 31), while retaining physical 1.20.1-1.20.4 adapter and mc26 priority-1100 tick-at-HEAD seam; standalone/Forge/NeoForge remain untouched. Static contract self-test passed 89 mutations including LoadingOverlayToast14; metrics passed in 27 seconds with 216 files / 16,544 LOC, 109 bridge files / 5,524 LOC (33.39%), zero duplicates, nine blocks / maximum 40 lines, and 18 platform target-key references. Focused offline six-boundary compileClientJava+sourcesJar reached BUILD SUCCESSFUL in 52 seconds across 48 tasks (39 executed, nine up-to-date); physical lower `6405DCD1...`, generated common `734966D6...`, generated modern `12A3E4CA...`, and physical mc26 `8D9A1069...` matched; classes existed. Independent final review passed. No runtime, all-53 parity, full matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `f74e0a7` (parent `ea10f52`): Phase E canonical preprocessed RuntimeResourceHash Fabric seam. Revert newest-first: revert later dependants, then `git revert f74e0a7`; then `aba98b3`, `c5b61e2`, `7efd43f`, `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates canonical guarded RuntimeResourceHash for exact Fabric 1.20.1 through 1.21.10 (17 targets), preserves raw Predicate/String in 1.21.11 and Identifier in mc26 as physical seams, and leaves standalone/Forge/NeoForge untouched. Static contract self-test passed 73 mutations including RuntimeResourceHash17; metrics passed in 27 seconds with 215 files / 16,496 LOC, 108 bridge files / 5,476 LOC (33.20%), zero duplicates, seven blocks / maximum 40 lines, and 18 platform target-key references. Focused offline 1.20.1/1.21.10/1.21.11/mc26 compileJava+sourcesJar reached BUILD SUCCESSFUL in 44 seconds across 24 tasks (18 executed, six up-to-date); active `F1726800...D9B7` matched generated output, physical 1.21.11 `ED80675E...AE83` and mc26 `532500D6...ABE6` matched, inactive generated output was absent, and classes existed. Independent final review passed. No runtime, all-53 parity, full matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `aba98b3` (parent `4846e15`): Phase E canonical preprocessed ReloadableResourceManager Fabric pre26 seam. Revert newest-first: revert later dependants, then `git revert aba98b3`; then `c5b61e2`, `7efd43f`, `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates the canonical guarded ReloadableResourceManager mixin for exact Fabric 1.20.1 through 1.21.11 (18 targets), transports it via generated `main`, and retains the mc26 physical StartupStatus/StartupTimings seam. Static contract self-test passed 61 mutations across 53 cells / 16 archive / 17 SharedZip / 18 ReloadableResourceManager / 19 Bitmap targets; metrics passed in 36 seconds with 214 files / 16,471 LOC, 107 bridge files / 5,451 LOC (33.09%), zero duplicates, six blocks / maximum 40 lines, and 18 platform target-key references. Focused offline 1.20.1/1.21.11/mc26 compileJava+sourcesJar reached BUILD SUCCESSFUL in 35 seconds across 18 tasks (12 executed, six up-to-date); active `ACD7634C...DDFD` matched generated output, mc26 `1D05F03C...A13C` matched retained physical source, and all classes existed. Independent final review passed. No runtime, all-53 parity, full matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `c5b61e2` (parent `c9349ea`): Phase E canonical preprocessed Bitmap provider Fabric client seam. Revert newest-first: revert later dependants, then `git revert c5b61e2`; then `7efd43f`, `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates the canonical BitmapProviderDefinitionMixin for Fabric client targets 1.20.1 through 26.1 (all 19), retains three physical variants for standalone/other loaders, uses generated `client/java`, and adds no target-key branch. Direct-contract self-test passed 49 mutations; metrics passed in 32 seconds with 213 files / 16,430 LOC, 106 bridge files / 5,410 LOC (32.93%), zero duplicates, five blocks / maximum 40 lines, and 18 platform target-key references. Focused 1.20.1/1.21.11/mc26 compileClientJava+sourcesJar passed in 32 seconds across 24 tasks (16 executed, eight up-to-date); each target had one `35EF7378...CB22` generated/source-JAR entry and one 2,655-byte compiled class. No runtime, full build, matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `7efd43f` (parent `deaf765`): Phase E canonical preprocessed SharedZip Fabric seam. Revert newest-first: revert later dependants, then `git revert 7efd43f`; then `4f82154`, then `b54a40f` if removing the preceding Fabric native pilot work. This activates canonical SharedZip preprocessing through registry-derived Fabric routes for 17 targets from 1.20.2 through 1.21.11, keeps normalized exact parity with the retained standalone shared source, keeps the mc26 physical accessor seam, and adds no target-key branch; 1.20.1 is not activated. Direct-contract self-test passed 38 mutations; metrics passed in 26 seconds with 212 files / 16,403 LOC, 105 bridge files / 5,383 LOC (32.82%), zero duplicates, four blocks / maximum 40 lines, and 18 platform target-key references; combined compile/sourceJar passed in 31 seconds across 14 tasks and follow-up sourcesJar was UP-TO-DATE PASS in 24 seconds; active/mc26-mixin/accessor SHA prefixes were `C4694D`, `1D281C`, and `F7131E`. No runtime, full build, matrix, release verification, or cross-loader native preprocessing proof; Phase E remains `PARTIAL`.
- `4f82154` (parent `b54a40f`): Phase E native loader-source metrics and compact archive-capture boundary. Revert newest-first: revert later dependants, then `git revert 4f82154`; revert `b54a40f` next only if removing the native Fabric pilot. This adds root loader branches to the handwritten metrics roots, fail-closed directive parsing, and the compact helper. Direct-contract self-test passed 22 mutations; metrics checkpoint passed in 34 seconds with 211 files / 16,366 LOC, 104 bridge files / 5,346 LOC (32.67%), zero duplicates, and three blocks / maximum 39 lines; boundary compile passed in 30 seconds; `sourcesJar` passed in 24 seconds; fresh devlibs held 67 files / 59 Java and canonical `BEAFDA...` matching generated output; independent review passed. No runtime, full build, matrix, or Minecraft acceptance; Phase E remains `PARTIAL`.
- `b54a40f` (parent `b66a0cc`): Phase E first native Stonecutter capability pilot. Revert newest-first: revert later dependants, then `git revert b54a40f`. This makes Fabric archive capture canonical for 16 active targets from 1.20.2 through 1.21.10, with the 1.20.5 seam; standalone, Forge, NeoForge, and legacy rollback paths remain. Direct-contract self-test passed 53 cells/16 native targets/21 mutations; offline 1.20.4+1.20.5 boundary compile passed in 37 seconds (daemon output after launcher timeout); `mc1_20_5 sourcesJar` passed in 25 seconds; fresh devlibs had 67 files/59 Java and exactly one canonical `2EB1...` entry matching generated output, with no wrong root/legacy source; independent review passed. No runtime, full build, matrix, or Minecraft acceptance; Phase E remains `PARTIAL`.
- `aa452ac` (parent `5d8deab`): Phase K hash-bound resumable exact-production PASS evidence. Revert newest-first: revert later dependants, then `git revert aa452ac`. This checkpoint validates the exact current matrix identity and PASS-line tokens, stored SHA-256 log/provenance evidence, controlled graceful exit, and safe numeric/evidence failures; it precomputes the matrix and scans prior results once, and a demoted cumulative PASS causes the controller to fail. AST, data-only valid controlled-exit plus 17-mutation/chronology self-test, and independent review passed. No Gradle, Minecraft, network, or matrix execution belongs to it.
- `5d8deab` (parent `ebdb90d`): Phase G exact shared configuration-screen base/wrapper and PackSelection routes. Revert after `aa452ac` and later dependants, then `git revert 5d8deab` to restore the preceding screen-route contract. AST, normal contract validation, mutation self-test, and independent review passed; no Gradle, runtime, or live UI evidence belongs to it.
- `eb14498` (parent `a15c4e0`): schema-2 compatibility-profile configuration contract. Revert newest-first: remove later dependants before `git revert eb14498`. This checkpoint adds the shared 50-field `PackForgeConfig.Cfg` v12 smoke baseline, permits exactly five safe boolean override keys (ZIP pool, font bitmap cache, atlas decode, atlas retry, startup executor tuning), and leaves all production defaults/default-off candidates unpromoted. Seven-script AST validation, 36/36 normal catalog validation, a self-test rejecting 22 catalog mutations plus Java default drift, and offline Fabric/Forge/NeoForge profile transport passed in 22.1 seconds; independent review passed. No Gradle, network, Minecraft, or runtime profile execution belongs to this checkpoint.
- `a80a83e77ed8190ad4de1e0c07665264af48f1c9`: expand the exact compatibility catalog to 21 `AVAILABLE`/`UNTESTED`, 12 `PENDING_METADATA`/`UNTESTED`, and three `UNAVAILABLE`. Revert its documentation checkpoint first, then this newest catalog checkpoint before earlier Phase I metadata/materializer work. Catalog normal/self-test passed 36 recipes and 19 mutations; 20 unique new pins were independently SHA-256/metadata verified and downloads were not retained. No Minecraft runtime or PASS profile exists; status remains `PARTIAL_NOT_RELEASE_READY`.
- `07b087fa1e3c862f086a25e29bab68eec37477d2`: aggregate `validateImplementationContracts` CI/registry gate and publication-time existing-manifest verification. Revert this newest checkpoint first, before `d833747`; 4/4 static contracts passed in 36.9 seconds without compilation, the focused Python manifest test and independent review passed, and workflow YAML received static inspection because a YAML parser was unavailable. No final-JAR manifest or Minecraft runtime proof belongs to this checkpoint.
- `d833747749ed665595231b6e72acc22f469b7e1a`: current-evidence documentation reconciliation. Revert after `07b087f` to restore the prior README and evidence documents. Independent documentation review passed, but the recorded state remains `PARTIAL_NOT_RELEASE_READY`; no build, package, compatibility profile, final-JAR verification, or Minecraft runtime was run.
- `3c01a50`: authoritative public direct graph for 53 loader distributions plus mandatory `validateStonecutterDirectContract`; JOptSimple registry metadata remains registry-owned. Revert this newest direct-build checkpoint first. Nested Gradle survives only as parity rollback oracle; no all-53 build, complete structural/package parity, runtime, or true Stonecutter-preprocessed source proof belongs to this checkpoint.
- `6257c35d50d04e4d874b1175d4808af52fbacdb1`: schema-2 compatibility-profile materialization across the exact-production controller, three loader smoke wrappers, and focused profile test. Revert this newest checkpoint first, before its parent or earlier Phase I catalog/reporter checkpoints, to restore selector-only profile handling. Offline self-test passed in 15.7 seconds, AST validation passed 5/5, and independent review passed; no network/download, Gradle, Minecraft, or runtime execution occurred. Seven metadata-`AVAILABLE` profiles remain `UNTESTED`; ImmediatelyFast-only profiles and nonempty configuration overrides intentionally fail closed pending dedicated path-marker and override transport.
- `0dc06bc2a6e684e7efd3d1df5f3f0f1aba6a7db0`: planned 1.20.2 target, Fabric/Forge bridge, Stonecutter node, and planned-target exclusion from the published 17-artifact aggregate. Revert after `862022c` if the feasibility cell is abandoned.
- `862022c40249155f1d43fb55d97f3c4cb132c136`: keep the 1.21.11 archive constructor hook version-local. Revert this first to restore the prior descriptor before reverting the 1.20.2 bridge.
- `051aaaca42bdc980a3260d1a6c6fcd4404f228b7`: complete all remaining Java 21 range candidates, capability floors, and exact runtime gates. Revert before removing their source-family definitions.
- `a3402866b217ac159d6a3cec70d3585028f79732`: latest historical full-runtime checkpoint, not the latest current-source checkpoint. It promotes all exact rows, adds the resumable 62-cell controller and per-version Fabric natives, consolidates canonical shared sources, and adds the source-metrics gate. Revert this first to restore per-adapter source ownership and the prior publication ledger after reverting newer dependants.
- `71f29b116f40e967996fa5e75e45aad89a7b0bb6`: initial historical continuation baseline. It adds the earlier phase audit and portable deterministic source/ownership/branch metrics with exact-duplicate enforcement. Revert this checkpoint to return to the earlier limited metrics task; it does not alter PackForge runtime behavior.
- `0c2fb7bcff5dd634254f3ad25b205b896acfe34e`: operation-level Quick Pack ownership and phase-safe loader metadata. Revert this checkpoint to restore the prior early whole-mixin suppression; do not treat its build/package proof as real Quick Pack runtime evidence.
- `3840a63bb8361cadd216ad15853efb4428ac29d1`: immutable compatibility-profile inputs, cross-loader additional-mod provenance, log-marker assertions, and evidence-bound matrix resume. Revert this harness checkpoint before reverting operation-level Quick Pack ownership; no live compatibility result is implied by this checkpoint alone.
- `8241e600d807f3900224e90a17d3467413d30ee1`: registry-derived Stonecutter graph with 19 source anchors, 53 loader distributions, and a separate 62-cell exact runtime ledger. Revert later direct-build work first, then revert this checkpoint to restore the prior manually enumerated Stonecutter project graph and settings; the retained delegated platform builds remain the rollback path until Phase E parity is proven.
- `6125316572a086369320616ec754a8076068e29a`: direct Fabric `mc1_21_1` Stonecutter pilot and registry-mode-aware root aggregation. Revert this checkpoint before the Phase D graph checkpoint to return that cell to delegated build, collection, verification, and clean ownership; no other loader cell is cut over by this commit.
- `68866ccffae9a06d404d5a791dd4e68d089f9486`: direct-build ownership for all 19 Fabric Stonecutter leaves plus the shared direct/parity helper. Revert this first to return to the verified single-cell Fabric pilot. This checkpoint has representative Java 17/21/25 and one 213-entry parity proof, not a completed all-19 aggregate or runtime matrix.
- `c28cc8cf62dbafa7cd3a52496f259848788c3a78`: direct Forge `mc1_20_1` legacy-remap and `mc1_21_1` modern-JarJar pilots. Revert this before the Fabric matrix/helper checkpoint if Forge direct packaging must return to delegation; no Forge-wide parity or runtime proof is implied.
- `2beb0bd821c51d37f2a4b93cb57293ce0be70d5f`: guarded direct NeoForge `mc1_20_2` UserDev and `mc1_21_1` ModDev pilots. Revert this before earlier direct-loader checkpoints if NeoForge must return to delegation. Package and structural proof exists, but actual NeoForge ZIP parity/runtime proof does not.
- `9b6e25512c4363829f3b1a5f86538cabf2797fa5`: registry-default direct ownership for all 53 loader leaves. Revert this first to return Forge and NeoForge to their verified pilots while keeping Fabric direct. The all-cell build/parity/runtime matrices remain deferred at this checkpoint.
- `782510c685db64a90d99e177c5d7531e07d9668f`: forward-compatible configuration persistence and duplicate option-ID enforcement. Revert this before older configuration-model work if unknown-field preservation must be removed; doing so can make later-schema fields disappear on the next save.
- `e0a644d3443fbecc37fef7537de96e5d9cee148a`: README publication declaration and exact loader table consistency gate. Revert this documentation contract before changing the canonical publication count or target keys outside the registry; no runtime behavior depends on it.
- `d21a2a5f0652b46afd92bde87725fb943e1842a3`: isolates README publication validation as an explicit contract. Revert before changing its task ownership or restoring the registry-integrated gate; no compile or runtime evidence belongs to this checkpoint.
- `4c76d6520080834a3c278bbefd7c0ce31189da84`: centralizes seven registry-owned source policies and all target assignments. Revert this first if policy interpretation mis-selects sources, then restore loader-local selection. Static golden comparison, independent review, and `validateStonecutterRegistry` (34 seconds, 53 leaves, 62 cells) passed; compile/package/runtime remain unproven.
- `839928cb7ee2be3ebcab73db859e0c2a5fc34a84`: shared modern configuration renderer extraction. Revert only after later integer-validation and documentation checkpoints; Fabric `mc1_21_10` and `mc1_21_11 compileClientJava` each passed in 10 seconds, but no live UI/client evidence exists.
- `35a6b4ca39b8afc0a9d1a096a76780b463ff2b5e`: integer validation parity and structural screen contract. Revert this first, then `839928c`, after later documentation. Focused Fabric test (five tests, zero failures/errors), legacy/26.x client compilation, and `Validate-ConfigScreenContract` (`bodies=3`, `wrappers=2`, `entryPoints=9`, `sharedTargets=14`) passed; structural proof is bounded and live UI/client runtime remains unverified.
- `5f668bc597ffdc094c91fda645fde19e7f0ed5f2`: catalog-backed compatibility-profile runner selection. Revert this newest Phase I checkpoint first to remove profile-ID selection and its duplicate/conflicting selector guards; then revert `b35a0b0` and `934c283` if the catalog foundation must also be removed. Runner AST and focused selector tests passed, but no profile was downloaded or launched.
- `b35a0b04305aed040d0ae6d9f203acb3a0f3d84f`: fail-closed profile pin/evidence validation. Revert after `5f668bc` to return to catalog-only validation. The positive fixture plus 14 rejected mutations prove validator behavior only; fake-pin/nonexistent-evidence retests likewise did not resolve real metadata.
- `934c283695a099593134e178bbe4b26f274ffe1a`: declarative 36-profile compatibility catalog. Revert last among the Phase I foundation commits. All entries remain `PENDING_METADATA` and `UNTESTED`; `AVAILABLE` materialization intentionally fails closed until exact metadata, hashes, and execution evidence exist. The direct catalog validator is invoked by the runner, not by a root Gradle/CI gate.
- `67a7bf5e57de4d35089983db00ae84328d530c7c`: existing release-manifest verifier. Revert after later documentation-only checkpoints and before its parent `1914912723bcfa19742d58e3bb549d2c203ab5b5`; this removes non-writing manifest verification and its self-test without changing generated artifacts. Generator/self-test and independent review passed, but the real verifier was not run: only 7/20 expected JARs existed and `build/libs/release/manifest.json` was absent. No release, full-matrix, or Minecraft runtime proof belongs to this checkpoint.
- `943354a4897011b62b4c4cab958afbc92fd16035`: atlas-retry shader guard policy. Revert after later documentation-only checkpoints to restore mc26 startup mutation of `atlasRetryEnabled`; focused policy/UI tests passed, but no shader-mod runtime or performance evidence exists and the feature remains default-off.
- `3168c0f713a475151b57d5ba1b735263a1121c34`: frozen Phase J candidate dispositions. Revert after later docs to remove the 11-candidate validator and restore the prior claims-only matrix. No production default changed; all promotion/runtime gates were intentionally left `NOT_RUN`.
- `5ee5f34bc8e56f28f134edab5f311f8797dea557`: environment-gated compatibility reporter. Revert after `2eccf28` and later Phase I runner work to remove the loader-observed mod/29-capability marker from core initialization. Focused tests passed; no profile execution belongs to this checkpoint.
- `2eccf28c7486c037b31465d8330e59a8ea907736`: exact Quick Pack/ImmediatelyFast pins and Resource Pack Unbounded availability records. Revert before `5ee5f34` to return the catalog to all-pending metadata. Seven profiles were metadata-available but remained `UNTESTED`; no third-party artifacts were downloaded or launched.
- `458616fb1d3eaa96623ce86dfed41749daf60e38`: deterministic 1.21.1 compatibility-fixture generator and contract manifest. Revert after `b116129` and later docs to remove fixture inputs; its self-test proves byte/CRC/manifest determinism only, not a Minecraft launch, profile result, cross-version fixture, or final-JAR acceptance.
- `b1161295bd3a41a0349841fd67ff3f0c1634c3bd`: refreshed source-metrics Markdown/JSON snapshot. Revert first among these two checkpoints to restore prior reported inventory only; `reportSourceMetrics` passed in 36 seconds with 210 files, 16,323 LOC, zero duplicate groups, 15 conditionals, three renderer bodies, two adapters, and 20 artifacts. No compile, package, or runtime proof belongs to this documentation checkpoint.

The unrelated `.github/ISSUE_TEMPLATE/bug-report.yml` edit remains unstaged and is not part of any rollback.
